import React from "react";
import { Typography } from "@material-ui/core";

const Logo = () => {
  return (
    <Typography
      variant="h6"
      color="primary"
      align="left"
      style={{ cursor: "pointer" }}
    >
      Netan Beauty
    </Typography>
  );
};

export default Logo;
