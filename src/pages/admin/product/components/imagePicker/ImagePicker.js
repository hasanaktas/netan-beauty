import React from "react";
import { Box, Typography } from "@material-ui/core";

const ImagePicker = () => {
  return (
    <Box>
      <Typography>Resim Sec</Typography>
    </Box>
  );
};

export default ImagePicker;
